import { EventPortal } from "@/components/EventPortal";

export const dynamic = "force-dynamic";
export const metadata = { title: "Event-Portal – TaxiOS" };

export default function EventPage() {
  return <EventPortal />;
}
