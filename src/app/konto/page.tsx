import { CustomerAccount } from "@/components/CustomerAccount";

export const dynamic = "force-dynamic";
export const metadata = { title: "Mein Konto – TaxiOS" };

export default function KontoPage() {
  return <CustomerAccount />;
}
